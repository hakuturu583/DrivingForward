# Ignore everything by default to minimize build context
**

# Keep Docker build files
!docker/Dockerfile
!docker/entrypoint.sh

# Keep only the rasterizer submodule needed for build
!models/gaussian/gaussian-splatting/submodules/diff-gaussian-rasterization/**

# Keep requirements for build-time install
!requirements.txt
